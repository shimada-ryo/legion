# Web Theme Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** legion web に CSS 変数ベースのデザイントークンと、Light / Dark / System の 3 mode を切替可能なテーマ機構を導入する。配色は Midnight Indigo、フォントは Inter (英) + Murecho (和) + JetBrains Mono の self-host バンドル。React Flow キャンバスもテーマに追従させる。

**Architecture:** `<html data-theme="light|dark">` に CSS 変数を切替えて適用。`ThemeProvider` (React Context) が `localStorage` と `matchMedia('(prefers-color-scheme: dark)')` を読み、`mode: 'light' | 'dark' | 'system'` と `resolved: 'light' | 'dark'` を管理。`TopNav` の `<ThemeToggle>` がクイック切替、`Settings` の `<AppearanceSection>` が 3 択明示選択を担う。外部ライブラリ非依存（@fontsource は asset 同梱のみ）。

**Tech Stack:** React 19, Vite 8, `@xyflow/react` 12.x, `@testing-library/react` 16.x + `happy-dom` 20.x, `bun test`.

**Spec reference:** [docs/dev/specs/2026-05-15_web_theme_design.md](../specs/2026-05-15_web_theme_design.md)
**Scope:** `packages/web/` のみ。`packages/runtime` / `packages/core` / `packages/server` / `packages/cli` は触らない。Phase 3 (runtime / Codex provider) との競合なし。

---

## File Structure

### Create

| Path | Responsibility | 予測行数 |
| --- | --- | --- |
| `packages/web/src/styles/tokens.css` | `:root` と `[data-theme="dark"]` のトークン宣言 | ~140 |
| `packages/web/src/styles/react-flow.css` | @xyflow クラスの上書き | ~60 |
| `packages/web/src/theme/ThemeProvider.tsx` | React Context, localStorage, matchMedia | ~80 |
| `packages/web/src/theme/ThemeToggle.tsx` | TopNav 用円形アイコンボタン | ~50 |
| `packages/web/src/theme/AppearanceSection.tsx` | Settings 用 3 択ラジオ | ~50 |
| `packages/web/test/theme/ThemeProvider.test.tsx` | unit test | ~80 |
| `packages/web/test/theme/ThemeToggle.test.tsx` | unit test | ~40 |
| `packages/web/test/theme/AppearanceSection.test.tsx` | unit test | ~40 |

### Modify

| Path | Change | 予測増減 |
| --- | --- | --- |
| `packages/web/package.json` | @fontsource-variable/{inter,murecho,jetbrains-mono} を deps に追加 | +3 |
| `packages/web/src/main.tsx` | font / CSS import、`<ThemeProvider>` で wrap | +8 |
| `packages/web/src/styles.css` | base reset + body の token 接続 | +5 / -1 |
| `packages/web/index.html` | FOUC 防止インラインスクリプト | +12 |
| `packages/web/src/components/TopNav.tsx` | inline hex を `var(--...)` に置換、`<ThemeToggle/>` をマウント | +5 / -4 |
| `packages/web/src/pages/Settings.tsx` | `<AppearanceSection/>` を挿入 | +5 |
| `packages/web/src/components/TemplateCanvas.tsx` | inline `background` を削除、`<Background color>` に dot color を渡す | +8 / -3 |
| `packages/web/src/components/template-canvas/styling.ts` | TYPE_BG を削除（bg は CSS で統一） | +0 / -10 |
| `docs/dev/manuals/user_test_manual.md` | テーマ切替手動確認の runbook 追加 | +30 |

---

## Pre-flight

- [ ] **Step P1: 仕様書とブランチを確認**

```bash
git branch --show-current
ls docs/dev/specs/2026-05-15_web_theme_design.md
```

Expected:
- branch: `worktree-design-color`
- spec ファイルが存在する

- [ ] **Step P2: ベースラインのテストと型チェックが通る**

```bash
bun install
bun run --filter=@legion/web typecheck
bun run --filter=@legion/web test
```

Expected: 両方とも green。

- [ ] **Step P3: 既存の inline 色使用箇所を把握しておく**

```bash
rg -n "#[0-9a-fA-F]{3,6}" packages/web/src --type tsx --type ts
```

Expected: TopNav.tsx, Settings.tsx, template-canvas/styling.ts に hex 値が見える（Task 7 / 8 / 9 で対応）。

---

## Task 1: CSS トークン宣言を作る

**Files:**

- Create: `packages/web/src/styles/tokens.css`

宣言ファイルのみで test は不要（次タスクで main.tsx に import してから視認確認）。

- [ ] **Step 1-1: tokens.css を作成**

`packages/web/src/styles/tokens.css`:

```css
/* legion web design tokens — Midnight Indigo */
/* Light is the default (no data-theme attribute or data-theme="light") */

:root,
:root[data-theme="light"] {
  /* Background */
  --bg-canvas: #f1f5f9;
  --bg-surface: #ffffff;
  --bg-elevated: #ffffff;

  /* Text */
  --fg-primary: #1e1b4b;
  --fg-muted: #475569;
  --fg-subtle: #94a3b8;

  /* Border */
  --border-default: #e2e8f0;
  --border-strong: #cbd5e1;

  /* Accent */
  --accent: #6366f1;
  --accent-hover: #4f46e5;
  --accent-fg: #ffffff;

  /* Status (semantic) */
  --status-running: #6366f1;
  --status-success: #10b981;
  --status-warning: #f59e0b;
  --status-error: #ef4444;

  /* React Flow */
  --node-bg: #ffffff;
  --node-border: #c7d2fe;
  --edge: #818cf8;
  --canvas-grid: #e0e7ff;

  /* Typography */
  --font-base:
    'Inter', 'Murecho',
    'Roboto', 'Noto Sans JP',
    -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui,
    'Hiragino Sans', 'Hiragino Kaku Gothic ProN',
    'Yu Gothic UI', 'Yu Gothic', 'Meiryo',
    sans-serif;

  --font-mono:
    'JetBrains Mono',
    'Fira Code', 'Cascadia Code',
    ui-monospace, 'SF Mono', 'Consolas', 'Menlo',
    monospace;
}

:root[data-theme="dark"] {
  --bg-canvas: #0c0a1a;
  --bg-surface: #1e1b4b;
  --bg-elevated: #2a2660;

  --fg-primary: #e0e7ff;
  --fg-muted: #a5b4fc;
  --fg-subtle: #6366f1;

  --border-default: #312e81;
  --border-strong: #4338ca;

  --accent: #a5b4fc;
  --accent-hover: #c7d2fe;
  --accent-fg: #1e1b4b;

  --status-running: #a5b4fc;
  --status-success: #34d399;
  --status-warning: #fbbf24;
  --status-error: #f87171;

  --node-bg: #1e1b4b;
  --node-border: #4338ca;
  --edge: #818cf8;
  --canvas-grid: #312e81;
}
```

- [ ] **Step 1-2: 行数を確認（予測 140 行に対して）**

```bash
wc -l packages/web/src/styles/tokens.css
```

Expected: 60〜80 行（実測。空行・コメント込み）。**予測との乖離があれば末尾の memo に記録。**

- [ ] **Step 1-3: コミット**

```bash
git add packages/web/src/styles/tokens.css
git commit -m "feat(web/theme): add design tokens for light/dark"
```

---

## Task 2: フォント依存追加と base styles の更新

**Files:**

- Modify: `packages/web/package.json`
- Modify: `packages/web/src/main.tsx`
- Modify: `packages/web/src/styles.css`

- [ ] **Step 2-1: @fontsource 依存を追加**

```bash
cd packages/web
bun add @fontsource-variable/inter @fontsource-variable/murecho @fontsource-variable/jetbrains-mono
cd ../..
```

Expected: `packages/web/package.json` の `dependencies` に 3 つ追加され、`bun.lock` が更新される。

- [ ] **Step 2-2: main.tsx で font と CSS を import**

`packages/web/src/main.tsx`:

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'

import '@fontsource-variable/inter'
import '@fontsource-variable/murecho'
import '@fontsource-variable/jetbrains-mono'

import './styles.css'
import './styles/tokens.css'

import App from './App'

const root = document.getElementById('root')
if (!root) throw new Error('#root not found')
createRoot(root).render(
  <StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </StrictMode>,
)
```

注: `ThemeProvider` でラップするのは Task 6（先に Provider を実装するため）。

- [ ] **Step 2-3: styles.css を body トークン接続に更新**

`packages/web/src/styles.css`:

```css
* { box-sizing: border-box; }

html, body, #root { height: 100%; margin: 0; }

body {
  font-family: var(--font-base);
  color: var(--fg-primary);
  background-color: var(--bg-canvas);
  transition: background-color 180ms ease, color 180ms ease;
}

@media (prefers-reduced-motion: reduce) {
  body { transition: none; }
}

.app { display: flex; flex-direction: column; height: 100%; }
.main { flex: 1; overflow: hidden; }
```

- [ ] **Step 2-4: 型チェック・テストが通る**

```bash
bun run --filter=@legion/web typecheck
bun run --filter=@legion/web test
```

Expected: 両方 green。テストは増えていないが、既存テストが回帰しないこと。

- [ ] **Step 2-5: コミット**

```bash
git add packages/web/package.json packages/web/src/main.tsx packages/web/src/styles.css
git commit -m "feat(web/theme): wire fonts and base body tokens"
```

注: `bun.lock` が更新されている場合は同コミットに含める。

```bash
git add bun.lock
git commit --amend --no-edit
```

---

## Task 3: ThemeProvider を TDD で実装

**Files:**

- Create: `packages/web/src/theme/ThemeProvider.tsx`
- Create: `packages/web/test/theme/ThemeProvider.test.tsx`

- [ ] **Step 3-1: 失敗するテストを書く**

`packages/web/test/theme/ThemeProvider.test.tsx`:

```tsx
import { describe, test, expect, beforeEach } from 'bun:test'
import { render, screen, act } from '@testing-library/react'
import { ThemeProvider, useTheme } from '../../src/theme/ThemeProvider'

function Probe() {
  const { mode, resolved, setMode } = useTheme()
  return (
    <div>
      <span data-testid="mode">{mode}</span>
      <span data-testid="resolved">{resolved}</span>
      <button onClick={() => setMode('dark')}>set-dark</button>
      <button onClick={() => setMode('system')}>set-system</button>
    </div>
  )
}

function mockMatchMedia(prefersDark: boolean) {
  const listeners = new Set<(e: { matches: boolean }) => void>()
  const mql = {
    matches: prefersDark,
    media: '(prefers-color-scheme: dark)',
    addEventListener: (_: 'change', fn: (e: { matches: boolean }) => void) => listeners.add(fn),
    removeEventListener: (_: 'change', fn: (e: { matches: boolean }) => void) => listeners.delete(fn),
    dispatchEvent: () => true,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
  }
  ;(window as any).matchMedia = (q: string) =>
    q.includes('dark') ? mql : { ...mql, matches: false }
  return {
    setPrefersDark(next: boolean) {
      mql.matches = next
      listeners.forEach((fn) => fn({ matches: next }))
    },
  }
}

describe('ThemeProvider', () => {
  beforeEach(() => {
    localStorage.clear()
    document.documentElement.removeAttribute('data-theme')
  })

  test('defaults to mode=system and resolves from prefers-color-scheme=dark', () => {
    mockMatchMedia(true)
    render(<ThemeProvider><Probe /></ThemeProvider>)
    expect(screen.getByTestId('mode').textContent).toBe('system')
    expect(screen.getByTestId('resolved').textContent).toBe('dark')
    expect(document.documentElement.getAttribute('data-theme')).toBe('dark')
  })

  test('reads existing localStorage value on mount', () => {
    localStorage.setItem('legion.web.theme', 'light')
    mockMatchMedia(true)
    render(<ThemeProvider><Probe /></ThemeProvider>)
    expect(screen.getByTestId('mode').textContent).toBe('light')
    expect(screen.getByTestId('resolved').textContent).toBe('light')
  })

  test('setMode persists to localStorage and updates DOM', () => {
    mockMatchMedia(false)
    render(<ThemeProvider><Probe /></ThemeProvider>)
    act(() => { screen.getByText('set-dark').click() })
    expect(localStorage.getItem('legion.web.theme')).toBe('dark')
    expect(document.documentElement.getAttribute('data-theme')).toBe('dark')
    expect(screen.getByTestId('mode').textContent).toBe('dark')
    expect(screen.getByTestId('resolved').textContent).toBe('dark')
  })

  test('mode=system follows OS preference change', () => {
    const ctrl = mockMatchMedia(false)
    render(<ThemeProvider><Probe /></ThemeProvider>)
    expect(screen.getByTestId('resolved').textContent).toBe('light')
    act(() => { ctrl.setPrefersDark(true) })
    expect(screen.getByTestId('resolved').textContent).toBe('dark')
  })

  test('explicit mode does not follow OS preference change', () => {
    const ctrl = mockMatchMedia(false)
    render(<ThemeProvider><Probe /></ThemeProvider>)
    act(() => { screen.getByText('set-dark').click() })
    act(() => { ctrl.setPrefersDark(false) })
    expect(screen.getByTestId('resolved').textContent).toBe('dark')
  })
})
```

- [ ] **Step 3-2: テストを実行して失敗を確認**

```bash
bun run --filter=@legion/web test test/theme/ThemeProvider.test.tsx
```

Expected: FAIL — `Cannot find module '../../src/theme/ThemeProvider'` または同等のエラー。

- [ ] **Step 3-3: ThemeProvider を実装**

`packages/web/src/theme/ThemeProvider.tsx`:

```tsx
import { createContext, useContext, useEffect, useState, type ReactNode } from 'react'

export type Mode = 'light' | 'dark' | 'system'
export type Resolved = 'light' | 'dark'

const STORAGE_KEY = 'legion.web.theme'

interface ThemeContextValue {
  mode: Mode
  resolved: Resolved
  setMode: (m: Mode) => void
}

const ThemeContext = createContext<ThemeContextValue | null>(null)

function readStoredMode(): Mode {
  try {
    const v = localStorage.getItem(STORAGE_KEY)
    if (v === 'light' || v === 'dark' || v === 'system') return v
  } catch {}
  return 'system'
}

function osPrefersDark(): boolean {
  try {
    return window.matchMedia('(prefers-color-scheme: dark)').matches
  } catch {
    return false
  }
}

function resolve(mode: Mode): Resolved {
  if (mode === 'system') return osPrefersDark() ? 'dark' : 'light'
  return mode
}

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [mode, setModeState] = useState<Mode>(() => readStoredMode())
  const [resolved, setResolved] = useState<Resolved>(() => resolve(readStoredMode()))

  // Apply data-theme to <html> whenever resolved changes.
  useEffect(() => {
    document.documentElement.setAttribute('data-theme', resolved)
  }, [resolved])

  // Persist mode and recompute resolved on every change.
  const setMode = (next: Mode) => {
    try { localStorage.setItem(STORAGE_KEY, next) } catch {}
    setModeState(next)
    setResolved(resolve(next))
  }

  // Subscribe to OS change only while mode === 'system'.
  useEffect(() => {
    if (mode !== 'system') return
    const mql = window.matchMedia('(prefers-color-scheme: dark)')
    const handler = (e: MediaQueryListEvent) => setResolved(e.matches ? 'dark' : 'light')
    mql.addEventListener('change', handler)
    return () => mql.removeEventListener('change', handler)
  }, [mode])

  return (
    <ThemeContext.Provider value={{ mode, resolved, setMode }}>
      {children}
    </ThemeContext.Provider>
  )
}

export function useTheme(): ThemeContextValue {
  const ctx = useContext(ThemeContext)
  if (!ctx) throw new Error('useTheme must be used inside <ThemeProvider>')
  return ctx
}
```

- [ ] **Step 3-4: テストを再実行して全部通る**

```bash
bun run --filter=@legion/web test test/theme/ThemeProvider.test.tsx
```

Expected: 5 tests pass。

- [ ] **Step 3-5: 全テストでリグレッションがないか確認**

```bash
bun run --filter=@legion/web test
bun run --filter=@legion/web typecheck
```

Expected: 両方 green。

- [ ] **Step 3-6: 行数記録とコミット**

```bash
wc -l packages/web/src/theme/ThemeProvider.tsx packages/web/test/theme/ThemeProvider.test.tsx
```

予測: ThemeProvider.tsx ~80、test ~80。乖離があれば末尾 memo に記録。

```bash
git add packages/web/src/theme/ThemeProvider.tsx packages/web/test/theme/ThemeProvider.test.tsx
git commit -m "feat(web/theme): add ThemeProvider with system pref follow"
```

---

## Task 4: ThemeToggle を TDD で実装

**Files:**

- Create: `packages/web/src/theme/ThemeToggle.tsx`
- Create: `packages/web/test/theme/ThemeToggle.test.tsx`

- [ ] **Step 4-1: 失敗するテストを書く**

`packages/web/test/theme/ThemeToggle.test.tsx`:

```tsx
import { describe, test, expect } from 'bun:test'
import { render, screen, fireEvent } from '@testing-library/react'
import { ThemeProvider } from '../../src/theme/ThemeProvider'
import { ThemeToggle } from '../../src/theme/ThemeToggle'

function setup(mode: 'light' | 'dark' | 'system' = 'system', prefersDark = false) {
  localStorage.clear()
  if (mode !== 'system') localStorage.setItem('legion.web.theme', mode)
  ;(window as any).matchMedia = (q: string) => ({
    matches: q.includes('dark') ? prefersDark : false,
    media: q,
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => true,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
  })
  return render(<ThemeProvider><ThemeToggle /></ThemeProvider>)
}

describe('ThemeToggle', () => {
  test('renders sun glyph when resolved=light', () => {
    setup('light')
    expect(screen.getByRole('button').textContent).toContain('☀')
  })

  test('renders moon glyph when resolved=dark', () => {
    setup('dark')
    expect(screen.getByRole('button').textContent).toContain('☾')
  })

  test('aria-label reflects the next state', () => {
    setup('light')
    expect(screen.getByRole('button').getAttribute('aria-label')).toBe('Switch to dark theme')
  })

  test('click flips to opposite of current resolved', () => {
    setup('light')
    fireEvent.click(screen.getByRole('button'))
    expect(document.documentElement.getAttribute('data-theme')).toBe('dark')
    expect(localStorage.getItem('legion.web.theme')).toBe('dark')
  })
})
```

- [ ] **Step 4-2: テスト失敗を確認**

```bash
bun run --filter=@legion/web test test/theme/ThemeToggle.test.tsx
```

Expected: FAIL — module not found。

- [ ] **Step 4-3: ThemeToggle を実装**

`packages/web/src/theme/ThemeToggle.tsx`:

```tsx
import { useTheme } from './ThemeProvider'

export function ThemeToggle() {
  const { resolved, setMode } = useTheme()
  const next = resolved === 'light' ? 'dark' : 'light'
  const glyph = resolved === 'light' ? '☀' : '☾'
  const label = `Switch to ${next} theme`

  return (
    <button
      type="button"
      onClick={() => setMode(next)}
      aria-label={label}
      title={label}
      style={{
        width: 24,
        height: 24,
        borderRadius: 12,
        border: '1px solid var(--border-default)',
        background: 'var(--bg-surface)',
        color: 'var(--fg-primary)',
        cursor: 'pointer',
        display: 'inline-flex',
        alignItems: 'center',
        justifyContent: 'center',
        fontSize: 13,
        lineHeight: 1,
        padding: 0,
      }}
    >
      {glyph}
    </button>
  )
}
```

- [ ] **Step 4-4: テストを再実行して通る**

```bash
bun run --filter=@legion/web test test/theme/ThemeToggle.test.tsx
```

Expected: 4 tests pass。

- [ ] **Step 4-5: 全体テストの確認**

```bash
bun run --filter=@legion/web test
bun run --filter=@legion/web typecheck
```

Expected: 全 green。

- [ ] **Step 4-6: 行数記録とコミット**

```bash
wc -l packages/web/src/theme/ThemeToggle.tsx packages/web/test/theme/ThemeToggle.test.tsx
```

予測: ThemeToggle.tsx ~50、test ~40。

```bash
git add packages/web/src/theme/ThemeToggle.tsx packages/web/test/theme/ThemeToggle.test.tsx
git commit -m "feat(web/theme): add ThemeToggle button"
```

---

## Task 5: AppearanceSection を TDD で実装

**Files:**

- Create: `packages/web/src/theme/AppearanceSection.tsx`
- Create: `packages/web/test/theme/AppearanceSection.test.tsx`

- [ ] **Step 5-1: 失敗するテストを書く**

`packages/web/test/theme/AppearanceSection.test.tsx`:

```tsx
import { describe, test, expect } from 'bun:test'
import { render, screen, fireEvent } from '@testing-library/react'
import { ThemeProvider } from '../../src/theme/ThemeProvider'
import { AppearanceSection } from '../../src/theme/AppearanceSection'

function setup(initial: 'light' | 'dark' | 'system' = 'system') {
  localStorage.clear()
  if (initial !== 'system') localStorage.setItem('legion.web.theme', initial)
  ;(window as any).matchMedia = () => ({
    matches: false,
    media: '',
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => true,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
  })
  return render(<ThemeProvider><AppearanceSection /></ThemeProvider>)
}

describe('AppearanceSection', () => {
  test('renders all three radios', () => {
    setup()
    expect(screen.getByLabelText('Light')).toBeDefined()
    expect(screen.getByLabelText('Dark')).toBeDefined()
    expect(screen.getByLabelText(/System/)).toBeDefined()
  })

  test('checks the radio matching current mode', () => {
    setup('dark')
    expect((screen.getByLabelText('Dark') as HTMLInputElement).checked).toBe(true)
    expect((screen.getByLabelText('Light') as HTMLInputElement).checked).toBe(false)
  })

  test('selecting Light updates state and persists', () => {
    setup('system')
    fireEvent.click(screen.getByLabelText('Light'))
    expect(localStorage.getItem('legion.web.theme')).toBe('light')
    expect(document.documentElement.getAttribute('data-theme')).toBe('light')
  })

  test('selecting System restores system mode', () => {
    setup('dark')
    fireEvent.click(screen.getByLabelText(/System/))
    expect(localStorage.getItem('legion.web.theme')).toBe('system')
  })
})
```

- [ ] **Step 5-2: テスト失敗を確認**

```bash
bun run --filter=@legion/web test test/theme/AppearanceSection.test.tsx
```

Expected: FAIL — module not found。

- [ ] **Step 5-3: AppearanceSection を実装**

`packages/web/src/theme/AppearanceSection.tsx`:

```tsx
import type { Mode } from './ThemeProvider'
import { useTheme } from './ThemeProvider'

const OPTIONS: { value: Mode; label: string }[] = [
  { value: 'light', label: 'Light' },
  { value: 'dark', label: 'Dark' },
  { value: 'system', label: 'System に追従' },
]

export function AppearanceSection() {
  const { mode, setMode } = useTheme()
  return (
    <section style={{ marginBottom: 24 }}>
      <h3 style={{ marginTop: 0, marginBottom: 8, fontSize: 14, color: 'var(--fg-muted)' }}>
        Appearance
      </h3>
      <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
        {OPTIONS.map((opt) => (
          <label key={opt.value} style={{ display: 'flex', alignItems: 'center', gap: 8, cursor: 'pointer' }}>
            <input
              type="radio"
              name="theme-mode"
              value={opt.value}
              checked={mode === opt.value}
              onChange={() => setMode(opt.value)}
            />
            <span>{opt.label}</span>
          </label>
        ))}
      </div>
    </section>
  )
}
```

- [ ] **Step 5-4: テスト通過を確認**

```bash
bun run --filter=@legion/web test test/theme/AppearanceSection.test.tsx
```

Expected: 4 tests pass。

- [ ] **Step 5-5: 全体テスト**

```bash
bun run --filter=@legion/web test
bun run --filter=@legion/web typecheck
```

Expected: 全 green。

- [ ] **Step 5-6: 行数記録とコミット**

```bash
wc -l packages/web/src/theme/AppearanceSection.tsx packages/web/test/theme/AppearanceSection.test.tsx
```

予測: AppearanceSection.tsx ~50、test ~40。

```bash
git add packages/web/src/theme/AppearanceSection.tsx packages/web/test/theme/AppearanceSection.test.tsx
git commit -m "feat(web/theme): add AppearanceSection 3-state radio"
```

---

## Task 6: ThemeProvider を App に組み込み + FOUC 対策

**Files:**

- Modify: `packages/web/src/main.tsx`
- Modify: `packages/web/index.html`

- [ ] **Step 6-1: main.tsx で App を `<ThemeProvider>` で wrap**

`packages/web/src/main.tsx`:

```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'

import '@fontsource-variable/inter'
import '@fontsource-variable/murecho'
import '@fontsource-variable/jetbrains-mono'

import './styles.css'
import './styles/tokens.css'

import { ThemeProvider } from './theme/ThemeProvider'
import App from './App'

const root = document.getElementById('root')
if (!root) throw new Error('#root not found')
createRoot(root).render(
  <StrictMode>
    <ThemeProvider>
      <BrowserRouter>
        <App />
      </BrowserRouter>
    </ThemeProvider>
  </StrictMode>,
)
```

- [ ] **Step 6-2: index.html に FOUC 防止スクリプトを追加**

`packages/web/index.html`:

```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>legion</title>
    <script>
      (function () {
        try {
          var mode = localStorage.getItem('legion.web.theme') || 'system';
          var resolved = mode === 'system'
            ? (matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light')
            : mode;
          document.documentElement.setAttribute('data-theme', resolved);
        } catch (e) {}
      })();
    </script>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 6-3: dev server で目視確認**

```bash
bun run --filter=@legion/web dev
```

ブラウザで `http://localhost:5173/` を開く。期待される動作:

- OS が dark mode のとき、初回表示が dark で出る（白チラつきなし）
- OS が light mode のとき、初回表示が light で出る
- TopNav / カードの背景色がトークンに従って変わる（dark なら濃紺、light なら淡灰青）

確認後 `Ctrl+C` で停止。

- [ ] **Step 6-4: 型チェック・テスト**

```bash
bun run --filter=@legion/web typecheck
bun run --filter=@legion/web test
```

Expected: 全 green。

- [ ] **Step 6-5: コミット**

```bash
git add packages/web/src/main.tsx packages/web/index.html
git commit -m "feat(web/theme): wire ThemeProvider and FOUC prevention script"
```

---

## Task 7: TopNav に ThemeToggle をマウント + inline hex の置換

**Files:**

- Modify: `packages/web/src/components/TopNav.tsx`

- [ ] **Step 7-1: TopNav を書き換え**

`packages/web/src/components/TopNav.tsx`:

```tsx
import type { CSSProperties } from 'react'
import { NavLink } from 'react-router-dom'
import { ThemeToggle } from '../theme/ThemeToggle'

export default function TopNav() {
  return (
    <nav
      className="topnav"
      style={{
        display: 'flex',
        alignItems: 'center',
        gap: 12,
        borderBottom: '1px solid var(--border-default)',
        padding: 8,
        background: 'var(--bg-surface)',
      }}
    >
      <span style={{ fontWeight: 600, marginRight: 16 }}>LEGION</span>
      <NavLink to="/templates" style={navStyle}>Templates</NavLink>
      <NavLink to="/instances" style={navStyle}>Instances</NavLink>
      <NavLink to="/settings" style={navStyle}>Settings</NavLink>
      <div style={{ marginLeft: 'auto' }}>
        <ThemeToggle />
      </div>
    </nav>
  )
}

const navStyle = ({ isActive }: { isActive: boolean }): CSSProperties => ({
  color: isActive ? 'var(--accent)' : 'var(--fg-muted)',
  textDecoration: 'none',
  fontWeight: isActive ? 600 : 400,
})
```

- [ ] **Step 7-2: 型チェック・テスト**

```bash
bun run --filter=@legion/web typecheck
bun run --filter=@legion/web test
```

Expected: 全 green。

- [ ] **Step 7-3: dev server で目視確認**

```bash
bun run --filter=@legion/web dev
```

ブラウザで `http://localhost:5173/templates` を開き：

- TopNav 右端に丸トグルが見える
- ☀ / ☾ アイコンが現在の resolved に応じて表示される
- クリックで Light↔Dark が即座に切替わる
- リンク色がトークン由来になっている（active = accent、inactive = fg-muted）

`Ctrl+C` で停止。

- [ ] **Step 7-4: コミット**

```bash
git add packages/web/src/components/TopNav.tsx
git commit -m "feat(web/theme): mount ThemeToggle in TopNav and tokenize colors"
```

---

## Task 8: Settings ページに AppearanceSection を追加

**Files:**

- Modify: `packages/web/src/pages/Settings.tsx`

- [ ] **Step 8-1: Settings を書き換え**

`packages/web/src/pages/Settings.tsx`:

```tsx
import { AppearanceSection } from '../theme/AppearanceSection'

export default function Settings() {
  return (
    <div style={{ padding: 16 }}>
      <h2>Settings</h2>
      <AppearanceSection />
      <p style={{ color: 'var(--fg-muted)' }}>
        Provider 設定 / auth 状態 / port 等は Phase 1.5 以降で実装します。
      </p>
    </div>
  )
}
```

- [ ] **Step 8-2: 型チェック・テスト**

```bash
bun run --filter=@legion/web typecheck
bun run --filter=@legion/web test
```

Expected: 全 green。

- [ ] **Step 8-3: dev server で目視確認**

```bash
bun run --filter=@legion/web dev
```

ブラウザで `http://localhost:5173/settings` を開き：

- "Appearance" ヘッダーと 3 つのラジオ（Light / Dark / System に追従）が見える
- 現在の選択にチェックが入っている
- ラジオを切替えると即座にテーマが変わる
- TopNav トグルでの変更がここに同期する（逆も同じ）

`Ctrl+C` で停止。

- [ ] **Step 8-4: コミット**

```bash
git add packages/web/src/pages/Settings.tsx
git commit -m "feat(web/theme): add AppearanceSection to Settings page"
```

---

## Task 9: React Flow のスタイル上書きと TemplateCanvas 接続

**Files:**

- Create: `packages/web/src/styles/react-flow.css`
- Modify: `packages/web/src/main.tsx`
- Modify: `packages/web/src/components/TemplateCanvas.tsx`
- Modify: `packages/web/src/components/template-canvas/styling.ts`

- [ ] **Step 9-1: react-flow.css を作成**

`packages/web/src/styles/react-flow.css`:

```css
/* Theme overrides for @xyflow/react. Loaded after @xyflow/react/dist/style.css. */

.react-flow__node {
  background: var(--node-bg);
  border: 1px solid var(--node-border);
  color: var(--fg-primary);
  border-radius: 6px;
  font-size: 13px;
  padding: 6px 10px;
}

.react-flow__node.selected {
  border-color: var(--accent);
  box-shadow: 0 0 0 3px color-mix(in srgb, var(--accent) 25%, transparent);
}

.react-flow__edge-path {
  stroke: var(--edge);
  stroke-width: 1.5;
}

.react-flow__edge.selected .react-flow__edge-path {
  stroke: var(--accent);
  stroke-width: 2;
}

.react-flow__background {
  background-color: var(--bg-surface);
}

.react-flow__controls {
  background: var(--bg-elevated);
  border: 1px solid var(--border-default);
  border-radius: 6px;
  overflow: hidden;
}

.react-flow__controls button {
  background: transparent;
  color: var(--fg-primary);
  border-bottom: 1px solid var(--border-default);
}

.react-flow__controls button:hover {
  background: color-mix(in srgb, var(--accent) 10%, transparent);
}

.react-flow__minimap {
  background: var(--bg-surface);
  border: 1px solid var(--border-default);
}

/* No transition on canvas internals — switching themes is instant. */
.react-flow,
.react-flow * {
  transition: none !important;
}
```

- [ ] **Step 9-2: main.tsx に react-flow.css の import を追加**

main.tsx の import セクションを更新:

```tsx
import './styles.css'
import './styles/tokens.css'
import './styles/react-flow.css'
```

（既存の `'@xyflow/react/dist/style.css'` は `TemplateCanvas.tsx` で import 済みなので、`react-flow.css` はその **後** に評価される順を保つ。main.tsx で import すれば最後にロードされて勝つ。）

- [ ] **Step 9-3: styling.ts から TYPE_BG を削除**

`packages/web/src/components/template-canvas/styling.ts`:

```ts
import type { TemplateNode, EdgeType } from '@legion/core'

export interface NodeStyle {
  border: string
  label: string
  shape?: 'rect' | 'diamond' | 'parallelogram'
}

const TYPE_BORDER: Record<TemplateNode['type'], string> = {
  trigger: '#888',
  role: '#0066cc',
  blackboard: '#aa00aa',
  'human-gate': '#cc8800',
  sink: '#666',
}

export function nodeStyleFor(node: TemplateNode): NodeStyle {
  const border = TYPE_BORDER[node.type]
  let label = `${node.id}`
  if (node.type === 'role') label = `${node.role}\n(${node.lifetime})`
  if (node.type === 'trigger') label = `${node.id} (${node.kind})`
  if (node.type === 'blackboard') label = `📋 ${node.id}`
  if (node.type === 'human-gate') label = `🙋 ${node.label}`
  if (node.type === 'sink') label = `${node.id} (${node.kind})`
  return { border, label }
}

export interface EdgeStyle {
  stroke: string
  animated: boolean
  label: string
}

const EDGE_COLOR: Record<EdgeType, string> = {
  triggers: '#0066cc',
  delegates: '#00aa66',
  publishes: '#aa00aa',
  subscribes: '#7700aa',
  reviews: '#cc6600',
  synthesizes: '#cc0066',
}

export function edgeStyleFor(type: EdgeType): EdgeStyle {
  return {
    stroke: EDGE_COLOR[type] ?? '#999',
    animated: type === 'triggers',
    label: type,
  }
}
```

変更点:
- `NodeStyle` から `background` を削除
- `TYPE_BG` 定数を削除
- `nodeStyleFor` 戻り値から `background` を削除

注: `TYPE_BORDER` と `EDGE_COLOR` の hex は両モードで視認できる彩度のため保持。タイプ別の区別はこの色で行う。

- [ ] **Step 9-4: TemplateCanvas を更新**

`packages/web/src/components/TemplateCanvas.tsx`:

```tsx
import { useEffect, useMemo, useState } from 'react'
import {
  ReactFlow,
  Background,
  Controls,
  type Node,
  type Edge,
} from '@xyflow/react'
import '@xyflow/react/dist/style.css'
import type { WorkflowTemplate } from '@legion/core'
import { nodeStyleFor, edgeStyleFor } from './template-canvas/styling'
import { layoutTemplate } from './template-canvas/layout'
import { useTheme } from '../theme/ThemeProvider'

export default function TemplateCanvas({ template }: { template: WorkflowTemplate }) {
  const positions = useMemo(() => layoutTemplate(template), [template])
  const { resolved } = useTheme()

  const [dotColor, setDotColor] = useState('')
  useEffect(() => {
    const cs = getComputedStyle(document.documentElement)
    setDotColor(cs.getPropertyValue('--canvas-grid').trim())
  }, [resolved])

  const nodes = useMemo<Node[]>(
    () =>
      template.nodes.map((n) => {
        const style = nodeStyleFor(n)
        const pos = positions[n.id] ?? { x: 0, y: 0 }
        return {
          id: n.id,
          position: pos,
          data: { label: style.label },
          style: {
            padding: 8,
            border: `2px solid ${style.border}`,
            borderRadius: 6,
            fontSize: 12,
            whiteSpace: 'pre-line',
            minWidth: 120,
            textAlign: 'center',
          },
        }
      }),
    [template, positions],
  )

  const edges = useMemo<Edge[]>(
    () =>
      template.edges.map((e, i) => {
        const style = edgeStyleFor(e.type)
        return {
          id: `${e.from}-${e.to}-${i}`,
          source: e.from,
          target: e.to,
          label: style.label,
          animated: style.animated,
          style: { stroke: style.stroke, strokeWidth: 2 },
          labelStyle: { fontSize: 10, fill: style.stroke },
        }
      }),
    [template],
  )

  return (
    <div style={{ width: '100%', height: '100%' }}>
      <ReactFlow
        nodes={nodes}
        edges={edges}
        fitView
        nodesDraggable={false}
        nodesConnectable={false}
      >
        <Background color={dotColor} />
        <Controls showInteractive={false} />
      </ReactFlow>
    </div>
  )
}
```

変更点:
- `useTheme` で `resolved` を購読し、`useEffect` で `--canvas-grid` を実値解決
- node style の `background` プロパティを削除（CSS で `var(--node-bg)` が当たる）
- `<Background>` に dot color を渡す

- [ ] **Step 9-5: 型チェック・テスト**

```bash
bun run --filter=@legion/web typecheck
bun run --filter=@legion/web test
```

Expected: 全 green。`TemplateCanvas.tsx` の既存テストがあれば、`nodeStyleFor` の戻り値型変更で影響する可能性あり。`bun test` で確認すること。

- [ ] **Step 9-6: dev server で目視確認**

```bash
bun run --filter=@legion/web dev
```

ブラウザで `/templates/:id`（既存テンプレートを開く）にアクセス：

- ノードの背景がテーマに合った色（Light: 白 / Dark: 濃紺）
- ノードの枠線が type 別の色（trigger=gray, role=blue, blackboard=magenta, human-gate=orange, sink=darker-gray）で表示される
- エッジが種類別の色で表示される（triggers=blue, delegates=green, publishes=magenta, ...）
- 背景ドットが Light で淡 indigo、Dark で濃 indigo
- Controls（ズーム/フィット）がテーマトークンに従って表示される
- TopNav トグルで Light↔Dark を切替えると、キャンバスも追従して変わる（dotColor も切替後に再計算される）

`Ctrl+C` で停止。

- [ ] **Step 9-7: コミット**

```bash
git add packages/web/src/styles/react-flow.css \
        packages/web/src/main.tsx \
        packages/web/src/components/TemplateCanvas.tsx \
        packages/web/src/components/template-canvas/styling.ts
git commit -m "feat(web/theme): theme-aware React Flow canvas"
```

---

## Task 10: user_test_manual.md にテーマ runbook を追加

**Files:**

- Modify: `docs/dev/manuals/user_test_manual.md`

- [ ] **Step 10-1: 既存マニュアルの構成を確認**

```bash
head -40 docs/dev/manuals/user_test_manual.md
```

末尾セクションへの追加が自然か、章番号を確認する。

- [ ] **Step 10-2: テーマ動作確認セクションを末尾に追加**

`docs/dev/manuals/user_test_manual.md` 末尾に以下を append:

```markdown
## Web UI: テーマ切替

### 初回アクセスの追従

1. OS の外観設定を Light にする
2. ブラウザのプライベートウィンドウで `http://localhost:5173/` を開く
3. **期待**: Light テーマで表示される（背景が淡 indigo、文字が濃紺）
4. プライベートウィンドウを閉じる
5. OS の外観設定を Dark にする
6. 再度プライベートウィンドウで開く
7. **期待**: Dark テーマで表示される（背景が濃紺、文字が淡 indigo）

### TopNav トグル

1. ブラウザで `/templates` などを開く
2. TopNav 右端の ☀/☾ アイコンをクリック
3. **期待**: 即座にテーマが切替わる。リロードしても維持される
4. もう一度クリックして元に戻ることを確認

### Settings の Appearance

1. `/settings` を開く
2. "Appearance" セクションに Light / Dark / System に追従 のラジオが見える
3. Light → Dark → System を順にクリック
4. **期待**: 各クリックで即座にテーマが反映、localStorage に保存される
5. System に追従 を選んだ状態で OS の Light/Dark を切替える
6. **期待**: 5 秒以内に web の表示も追従する

### FOUC（チラつき）の確認

1. localStorage に `legion.web.theme = 'dark'` をセット（DevTools の Application タブで）
2. ページをリロード（Ctrl+R）
3. **期待**: 一瞬たりとも白背景が見えず、ダーク背景で描画される

### React Flow キャンバス

1. `/templates/:id` を開く（既存テンプレートを表示）
2. テーマを Light に切替
3. **期待**: ノード白背景、枠線がタイプ別の色、背景に淡 indigo のドット
4. テーマを Dark に切替
5. **期待**: ノード濃紺背景、枠線・エッジは同じ色味、背景ドットが濃 indigo
6. Controls（左下のズーム）の色がテーマに追従していることを確認
```

- [ ] **Step 10-3: コミット**

```bash
git add docs/dev/manuals/user_test_manual.md
git commit -m "docs(manuals): add theme switching runbook"
```

---

## Final verification

- [ ] **Step F-1: 全テストの最終確認**

```bash
bun run --filter=@legion/web typecheck
bun run --filter=@legion/web test
```

Expected: 両方とも green。新規テスト計 13 件以上（ThemeProvider 5 / ThemeToggle 4 / AppearanceSection 4）。

- [ ] **Step F-2: dev server で end-to-end の動作確認**

`docs/dev/manuals/user_test_manual.md` 内の "Web UI: テーマ切替" セクションの各 runbook を順に実行し、すべて期待通りに動くことを確認。

- [ ] **Step F-3: コミット履歴の確認**

```bash
git log --oneline worktree-design-color ^main
```

Expected: 10 commits（pre-flight 除く Task 1〜10 が 1 commit ずつ）。

- [ ] **Step F-4: 行数予測の精度を末尾に記録**

`docs/dev/plans/2026-05-15_web_theme.md` の末尾に "## 実装後 memo" セクションを追加し、各ファイルの予測行数と実測値の対比を書く。差が大きい (>30%) 項目があれば原因を 1 行付記。

---

## Implementation notes

### TYPE_BG 削除の理由

spec §4.1 は `--node-bg` を単一トークンとして定義しており、現状の `TYPE_BG`（trigger=gray, role=light-blue, blackboard=light-purple, human-gate=light-yellow, sink=light-gray）の per-type 背景は spec の意図と整合しない。タイプ別の視覚区別は `TYPE_BORDER` と label の絵文字（📋 / 🙋）に集約する。本 plan で TYPE_BG を削除する。

将来 per-type 背景を復活させる場合は、`--node-bg-trigger` 等の追加トークンを spec に追加してから対応する。

### TYPE_BORDER / EDGE_COLOR の hex 保持の理由

spec §8 は `.react-flow__node { border: 1px solid var(--node-border) }` を CSS rule で定義するが、`TemplateCanvas` は inline `style.border` で per-type の hex を書いているため inline が優先される。これは意図通りで、type 別の意味色を保つために許容する。`TYPE_BORDER` / `EDGE_COLOR` の hex 値（#888, #0066cc, #aa00aa, #cc8800, #666, #00aa66, #7700aa, #cc6600, #cc0066）は彩度が十分で、Light/Dark 両方の背景に対し視認性がある。

### bun.lock のコミットタイミング

Task 2 で `bun add` が `bun.lock` を変更する。同タスクの最終コミットに含めること。

### dev server 確認の代替

agentic worker が `bun run dev` をフォアグラウンドで実行できない環境では、`bun run --filter=@legion/web build` だけ確認して目視を後回しにしても良い。ただし最終的に Final verification F-2 で人間（または別ターミナル）が runbook を実行する必要がある。
